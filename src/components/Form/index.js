import React from "react";
import Input from "../Input";
import Button from "../Button";

const Form = () => (
  <div>
    <Input inputName={"Test"} />
    <Button />
  </div>
);

export default Form;
